import {expect, test} from "vitest";
import {App} from "./App.ts";
import {API} from "./API.ts";

test("Test whether the quiz is generating based on difficulty level", () => {
  expect();
});

test("Test whether the score is calculating right", () => {
  expect(checkAnswer());
});
